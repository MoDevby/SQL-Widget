<UserControl x:Class="Sql_Widget.Controls.FavoriteControl"
			xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 

			xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" FontSize="20"
			FontFamily="{DynamicResource MaterialDesignFont}" Background="{DynamicResource MaterialDesignPaper}">
	<UserControl.Resources>
		<DataTemplate x:Key="FavoriteElement">
			<Grid Margin="5" VerticalAlignment="Center"  HorizontalAlignment="Stretch">
				<Grid.RowDefinitions>
					<RowDefinition/>
					<RowDefinition/>
				</Grid.RowDefinitions>
				<StackPanel Grid.Row="0" HorizontalAlignment="Left" Orientation="Horizontal" Margin="5,0">
					<TextBlock Text="{Binding Description}" VerticalAlignment="Center" />
					<Button Command="{Binding EditDescriptionCommand}" CommandParameter="{RelativeSource Self}"/>
				</StackPanel>
				<StackPanel Grid.Row="1" HorizontalAlignment="Left" Orientation="Horizontal" Margin="5,0">
					<TextBlock Text="{Binding Query}" VerticalAlignment="Center" />
					<Button Command="{Binding EditQueryCommand}" CommandParameter="{RelativeSource Self}"/>
				</StackPanel>
			</Grid>
		</DataTemplate>
	</UserControl.Resources>
	<Grid>
		<ListBox Margin="5" ItemTemplate="{StaticResource FavoriteElement}" ItemsSource="{Binding FavoriteItems}"
			 SelectedItem="{Binding SelectedFavorite5 Element,Mode=OneWayToSource}"/>
	</Grid>
</UserControl>
